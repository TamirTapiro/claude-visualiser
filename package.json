{
  "name": "claude-visualiser-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Build-time dependencies for claude-visualiser",
  "type": "module",
  "repository": "https://github.com/TamirTapiro/claude-visualiser",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "d3": "^7.9.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  }
}
